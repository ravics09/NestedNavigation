import React from 'react';
import {View, Text} from 'react-native';

const NetworkScreen = () => (
  <View style={{flex: 1, justifyContent: 'center', alignItems: 'center'}}>
    <Text>This is Network Screen </Text>
  </View>
);
export default NetworkScreen;
